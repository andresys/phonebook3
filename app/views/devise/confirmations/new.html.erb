<span class="block m-6 text-xl text-center leading-5 font-light text-gray-400">Для подтверждения вашего email-адреса, заполните следующую форму</span>

<div class="card py-8 px-4 sm:px-10">
  <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name)) do |f| %>
    <div>
      <%= f.label :email, "Email-адрес", class: "block text-sm font-medium leading-5 text-gray-700 tracking-[2px]" %>
      <div class="mt-1 relative rounded-md shadow-sm">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "user@adm.tver.ru", class: "appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400 sm:text-sm sm:leading-5",
          value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email) %>
      </div>
    </div>

    <div class="mt-6">
      <span class="block w-full rounded-md shadow-sm">
        <button type="submit" class="btn-primary w-full flex justify-center">
          Выслать инструкцию
        </button>
      </span>
    </div>
  <% end %>

  <% if controller_name != 'sessions' %>
    <p class="mt-4 text-center text-sm text-gray-900 max-w">Уже есть аккаунт?
      <%= link_to "Войди в систему", new_session_path(resource_name), class: "link font-medium" %>
    </p>
  <% end %>
</div>
